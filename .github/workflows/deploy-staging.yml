name: Deploy Staging

on:
  workflow_run:
    workflows: ["Provision Staging Infrastructure"]
    types: [completed]
    branches: [staging]

jobs:
  deploy-staging:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: staging

