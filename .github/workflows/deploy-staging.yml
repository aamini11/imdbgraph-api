name: Deploy Staging

on:
  workflow_run:
    workflows: ["Provision Staging Infrastructure"]
    types: [completed]
    branches: [staging]

jobs:
  deploy-staging:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/deploy.yml
    with:
      environment: staging

