apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: imdbgraph-tls
  namespace: cert-manager
spec:
  issuerRef:
    name: letsencrypt
    kind: Issuer
    group: cert-manager.io
  dnsNames:
    - api.imdbgraph.org
  secretName: imdbgraph-tls